paralogs.pl is main perl script
this takes inputfile name and database name and calls formatdb , then
blastall, the calls blastparser.pl to parse the blastall output
blastall output is stored in inputfile.txt.out where inputfile.txt is name of
inputfile
